package com.glaf.apps.trip.action;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class TripAction extends TripBaseAction {
	protected final static Log logger = LogFactory.getLog(TripAction.class);

	public TripAction() {

	}

}
